# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: release_smithy_ssdk
on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
jobs:
  release_npm:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          registry-url: https://registry.npmjs.org
      - name: who am i
        env:
          NODE_AUTH_TOKEN: ${{ secrets.TOKEN }}
        run: npm whoami
      - name: Install dependencies
        run: yarn install --check-files --frozen-lockfile
      - name: Build SSDK
        run: yarn build
        working-directory: src/packages/my-api/build/smithy/source/typescript-ssdk-codegen
      - name: Pack artifact
        run: yarn pack --filename smithy-ssdk.tgz
        working-directory: src/packages/my-api/build/smithy/source/typescript-ssdk-codegen
      - name: Extract artifact
        run: mkdir repo && tar -xzf src/packages/my-api/build/smithy/source/typescript-ssdk-codegen/smithy-ssdk.tgz -C repo --strip-components=1
      - name: Patch version
        run: jq '.version = "${{ inputs.version }}"' package.json > tmp.json && mv tmp.json package.json && cat package.json | grep version
        working-directory: repo
      - name: Remove prepack
        run: jq 'del(.scripts.prepack)' package.json > tmp.json && mv tmp.json package.json
        working-directory: repo
      - name: Publish to npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.TOKEN }}
        run: npm publish --access public
        working-directory: repo
  release_github:
    name: Publish GitHub Release
    needs: release_npm
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release create "v${{ inputs.version }}" --title "v${{ inputs.version }}" --notes "Automated release for SSDK" src/packages/my-api/build/smithy/source/typescript-ssdk-codegen/smithy-ssdk.tgz
